(() => {'use strict';const I=window.GP_BUILD_IDENTITY;if(!I)throw new Error('Shared build identity missing');const KEY=I.eventLogKey;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function rows(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}}async function verify(){const box=document.getElementById('version-check');try{const endpoint=new URL('./api/version',location.href);endpoint.searchParams.set('build',I.buildId);const r=await fetch(endpoint,{cache:'no-store',headers:{Accept:'application/json'}});const d=await r.json();const ok=r.ok&&d.version===I.version&&d.build_id===I.buildId&&d.api_contract===I.apiContract&&d.title_evidence===true;box.className=`diagnostic ${ok?'done':'error'}`;box.innerHTML=`<span><strong>${ok?'Versionen und Titeldiagnose konsistent':'Versionsabweichung'}</strong></span><span>Eventlog ${I.version}/${I.buildId} · Worker ${d.version||'?'}/${d.build_id||'?'}</span><span>Diagnoseschema: ${d.coverage_schema||'nicht verfügbar'} · Ausschnitt ${d.article_excerpt_limit||'?'} Zeichen</span>`;}catch(e){box.className='diagnostic error';box.textContent=`Versionsprüfung fehlgeschlagen: ${e?.message||e}`;}}function render(){const data=rows().slice().reverse();const coverage=data.filter(x=>x.type==='coverage_diagnostics');const malformed=coverage.reduce((n,x)=>n+Number(x.malformed_count||0),0);document.getElementById('log-summary').innerHTML=`<span>${data.length} Ereignisse</span><span>${coverage.length} Diagnoseblöcke</span><span>${malformed} fehlerhafte Karten</span><span>${data[0]?new Date(data[0].time).toLocaleString('de-DE'):'Noch leer'}</span><span>Build ${I.buildId}</span>`;document.getElementById('event-log').innerHTML=data.map(x=>`<div class="diagnostic"><span><strong>${esc(new Date(x.time).toLocaleString('de-DE'))}</strong></span><span>${esc(x.type)}</span><span>${esc(x.message)}</span>${Object.entries(x).filter(([k])=>!['time','epoch','type','message','signature'].includes(k)).map(([k,v])=>`<span>${esc(k)}: ${esc(typeof v==='object'?JSON.stringify(v):v)}</span>`).join('')}</div>`).join('')||'<div class="diagnostic"><span>Noch keine Ereignisse protokolliert.</span></div>';}document.getElementById('refresh-log').onclick=()=>{render();verify()};document.getElementById('clear-log').onclick=()=>{localStorage.removeItem(KEY);render()};document.getElementById('copy-log').onclick=async()=>{await navigator.clipboard.writeText(JSON.stringify(rows(),null,2));document.getElementById('copy-log').textContent='Kopiert'};render();verify();setInterval(render,3000);})();
