# Changelog 1.2.2

Transport-only Vinted production fix. Removes the hard public Browser PoC hostname from the GenericParser adapter and replaces it with a Cloudflare Service Binding. Adds request-scoped binding propagation and strict live verification. Existing Kleinanzeigen implementation is unchanged.
