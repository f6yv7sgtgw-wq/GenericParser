# GenericParser 0.45.2 Build 7 — Paid Worker

This build removes Free Worker pacing/protection delays while preserving the current Build 6 Evercade compatibility and the live-proven 0.45.0 search runtime.

Removed waits:
- new-search cooldown: 2000 ms -> 0 ms
- adaptive normal packet delay: -> 0 ms
- retry waits: 15/30/60 s -> immediate retries, same retry count
- auto-resume quiet period: 90 s -> effectively immediate (1 ms because the legacy recovery loader uses an `||` fallback)
- auto-resume health interval: 15 s -> effectively immediate (1 ms)

Unchanged: search_service_v0450, 0.44.4 core, 0.44.6.5 operational reference, matching, ranking, pagination, extraction, seven-result work packets and generic-parser-module-v1.
