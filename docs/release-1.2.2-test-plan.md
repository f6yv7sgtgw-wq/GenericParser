# Test plan 1.2.2

CI must prove unit/regression compatibility first. Production deployment must then prove health identity and a real `Evercade` multi-source request. Release acceptance requires Kleinanzeigen enabled, Vinted enabled, Vinted status `ok`, Vinted strategy `service-binding`, and at least one normalized Vinted listing.
